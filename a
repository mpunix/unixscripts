1.add hostgroup id
hammer --ouput jason hostgroup list --search 

2. register host group details
satellite_hostgroup_jason: satellite_hostgroup.stdout | from_jason| first

satellite_hostgroup_jason[id] is not defined

3. look fro subnet id
hammer --output jason subnet list --search "network={{ansible_default_ipv4['network'] }}"

4. satellie location id
hammer --output jason location list --search name={{ sat_location}}

5. create host entry
hammer host create --ask-root-password no
hostgroup-id
--interface="primary=true, mac , ip provision-true
--domain
--name
--organizaton 1
--paramters="kt_activation_keys"

6. host subnet id

7. activatoin key to host

